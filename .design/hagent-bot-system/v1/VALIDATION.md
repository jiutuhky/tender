# 设计样片检查记录

检查日期：2026-09-07。环境：当前本地 Chromium，经 browser-use / CDP 交互；独立 HTTP 静态预览。以下只适用于本目录的设计播放器。

## 已通过

| 检查 | 结果 |
|---|---|
| JavaScript 语法 | `node --check design.js` 通过 |
| 全部目录交互 | 通过真实按钮点击逐个选择 36 个唯一条目；主预览路径无 NaN / undefined / Infinity |
| 快速切换 | 阅读→编写→阅读→思考→检索→编写，以 240ms 间隔切换后进入等待确认；路径有效，未重挂载角色 |
| 确认门槛 | 等待确认情境在 awaiting-input 停住，帧循环落定；点击“模拟已确认并恢复”后才继续 |
| 完整交付 | 自动情境最终进入 completed |
| 中断与恢复 | 自动情境经 blocked / reconnecting / recover，最终进入 completed |
| 部分完成 | 自动情境最终进入 partial，保留注意标记 |
| 暂停 | 点击暂停后间隔取样的姿态完全一致；继续后恢复 |
| 键盘 | 原生按钮获取焦点后按空格可暂停与继续 |
| 事件收尾 | 交付完成单次动作落定后，时间与姿态不再变化，无持续 rAF |
| 系统减少动效 | CDP 动态切换系统偏好后，页面立即采用代表姿态，帧循环停止；恢复偏好后可继续 |
| 主题 | 浅/深主题按钮有效；主角色、状态标记、文字与示意图跟随主题 |
| 布局 | 1120 / 736 / 360 / 320px 均无页面横向溢出。首次 320px 检查发现实际尺寸行越界，已缩小间距；复查全部宽度无越界文字、按钮、SVG |
| 预览可见性 | 选择目录条目后返回主预览位置，可直接看到动作 |
| 文件结构 | HTML 无重复 ID；本地脚本、样式、规范与检查记录链接存在 |

## 截图

- [默认预览](preview.png)
- [桌面浅色](preview-1120-light.png)
- [桌面深色](preview-1120-dark.png)
- [窄屏浅色](preview-360-light.png)
- [窄屏深色](preview-360-dark.png)

截图用于布局与主题记录；实际运动请打开 [交互样片](index.html)。

## 明确边界

- 未接入 SSE、真实人工审批、重连、暂停、取消或子代理数据；情境中的确认只控制演示播放器。
- 快速切换按钮故意绕过业务状态驻留，用于观察表现层连续性；规范中的 180ms 候选稳定、600ms 最低驻留需在真实业务投影中实现。
- 未做真机触屏、跨浏览器、实际读屏软件或产品负载下的性能验收。可访问名称、原生控件与键盘操作已检查；这不等于完整无障碍认证。
- 未修改 frontend / hagent 产品代码，因此本轮没有运行产品 typecheck / lint；确认接入后按仓库质量门运行。
- 浏览器检查是设计样片的功能与布局检查；没有把代码隐患都描述为已复现故障，也没有把性能目标描述为已达到的成绩。
