Status: resolved
Type: task

# 修复数据库连接句柄积累

后端进程达到 1024 个文件句柄上限，其中约 995 个指向 sessions.db 及 WAL。
Session、Project、Lease、Run 与 SandboxEvent 存储误以为连接事务上下文会关闭连接。

使用 closing 包裹原事务上下文，保留提交和回滚语义，保证成功与异常路径关闭连接。
验证重复查询、写入失败后的关闭与数据保留，以及相关存储回归测试。

## 验证结果

- 修复前禁用循环 GC 后，100 次 RunStore 查询使句柄从 5 增长至 105。
- 存储与生命周期回归：49 项通过，包括五类存储共 1500 次查询与异常写入关闭。
- 项目、会话、资产接口回归：39 项通过，使用独立测试数据库回退路径。
- Docker 集成：8 项通过；SmolVM 实际虚拟机全链基础验证：1 项通过。
- 后端已平滑重启，连续 300 次实际项目查询全部成功，句柄保持 17 → 17。
- 后端健康检查与前端同源项目代理均正常；新进程仍使用默认 1024 句柄上限。
- 完整 server 目录回归曾长时间未结束，已终止，未计为通过；改用上述相关测试完成验证。

## 排查记录

广泛接口回归中的既有健康测试未设置独立数据库，会使用默认运行库并将残留
running Run 标记为重启中断。发现后终止该轮测试，后续指定隔离的数据库与工作区。
发生时任务已经因资源耗尽报错；未修改任务产物。实际重启也会执行未完成 Run 的中断恢复。
