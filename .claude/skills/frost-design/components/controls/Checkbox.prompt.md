# Checkbox

18px rounded box with a blue gradient fill and white check when on. Supports an `indeterminate` (mixed) state and an optional trailing label.

```jsx
<Checkbox defaultChecked label="引用原文页码" />
<Checkbox indeterminate label="全选" />
```

Use in forms and selection lists. For instant-effect settings, use Switch.
