# PDF 原文溯源验证记录

验证日期：2026-09-08。实现契约见 [原文溯源契约](原文溯源契约.md)。测试使用独立数据库、项目工作区和二进制存储；真实 PDF 通过上传 HTTP 接口进入入库管线。浏览器核验条目由测试程序构造，引用实际 OCR 映射，本记录不评价大模型提取准确率。

## 完整文档入库

| 样本 | 页数 | 映射块数 | 有映射页面 | 跨页块数 |
|---|---:|---:|---:|---:|
| 信保体系建设数据资源池项目招标文件 | 60 | 773 | 60 | 11 |
| 某部数据资源体系建设及大数据分析应用项目（数据资源体系建设）一包招标文件 | 69 | 823 | 69 | 10 |

两份样本都是带文本层的 A4 竖版 PDF。额外构造并上传 7 页文档，覆盖裁切偏移、0/90/180/270 度旋转、横版、UserUnit=2 和扫描页，避免把样例形态当作通用假设。

## 浏览器检查

Chrome 与 Firefox 分别遍历两份文件的全部 129 页。逐页确认 Canvas 完成渲染，未出现页面渲染错误；本次可见窗口最多保留 4 个 Canvas。检查了跨页区域步进、页码跳转、适应宽度、适应页面、缩放、窗口变化、关闭重开、Esc 逐层退出与文字选择复制。

- 页面与 SVG 覆盖层的显示边界误差为 0 CSS 像素；纯函数测试另检查矩形坐标换算。
- 在第 50 页切换适应页面及调整窗口后仍停留在第 50 页。
- 两个浏览器都通过 7 页补充文档检查；旋转与页面单位变化时，文本选择层的中心仍位于对应 OCR 区域中。扫描页可显示并标注。
- DPR=2 下通过快速区域切换、连续缩放和核验状态更新检查。
- 模拟 schema 1 映射时保留 PDF 阅读并停止高亮；恢复映射后重试可重新定位。
- 模拟 PDF 缺失时显示错误与下载入口；同源代理验证 Range 206 和无效范围 416。
- 浏览器页面异常监听未记录未处理异常；Tab 焦点保持在原文预览层内。

Edge 与 Safari 未独立实测。WebKit 浏览器已下载，但环境缺少 GTK、GStreamer 等系统库，安装需要当前不可用的系统权限，因此未将其记为通过。

## 自动检查

前端 `pnpm typecheck`、`pnpm lint`、`pnpm build` 通过；`pnpm test:trace` 4 项与 `pnpm test:bot` 18 项通过。

以下后端集合共 123 项通过：

```bash
hagent/.venv/bin/python -m pytest \
  hagent/tests/ingest \
  hagent/tests/server/test_document_carriers_api.py \
  hagent/tests/server/test_pdf_upload_and_progress.py \
  hagent/tests/server/test_project_files_api.py \
  hagent/tests/assets/test_documents.py \
  hagent/tests/test_file_tools_readonly_sources.py -q
```

覆盖 Triton 封装与业务错误、批次漏页后恢复正确页序、版本绑定、同名文件版本保留、已注册来源防覆盖、原件下载、分段读取、项目隔离、预览几何、跨页对齐失败、无效矩形、部分页失败、全部页失败及模型启动前等待入库。真实 HPS 跨页响应已保留为离线回放夹具。
